const startApp = ()=>{
    try {
        const name = "risano"
        name = "budi"
    } catch (error) {
        console.log('catch dijalankan');
        console.log(error.message);
    }
}

startApp()